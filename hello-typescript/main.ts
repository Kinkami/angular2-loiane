var minhaVar = 'minha variavel'

function minhaFunc(x, y) {
    return x + y;
}

//ES6 2015
let num = 2;
const PI = 3.14;

//Arrow functions
var numeros = [1, 2, 3];
numeros.map(valor => valor * 2); //ES2015


class Matematica {
    soma(x, y) {
        return x + y;
    }
}

var n1:any = 'foo';

n1 = 4;